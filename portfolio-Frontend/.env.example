# Base URL of the future Spring Boot REST API.
# Example for local development against Spring Boot:
VITE_API_BASE_URL=http://localhost:8080/api

# Set to "true" to force the app to use mock data even if VITE_API_BASE_URL is set.
VITE_USE_MOCKS=false
